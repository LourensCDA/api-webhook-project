[project]
name = "api-webhook-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.18.4",
    "fastapi[standard]>=0.128.0",
    "python-dotenv>=1.2.1",
    "sqlmodel>=0.0.33",
]
